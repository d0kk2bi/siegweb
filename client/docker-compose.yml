version: "3"
services:
    client:
        container_name: "client"
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - "$PWD/client:/client"
        command: bash
        stdin_open: true
        tty: true
